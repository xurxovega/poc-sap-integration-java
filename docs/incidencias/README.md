# Incidencias y post-mortems

Rastro de lo que se rompió, por qué, cuánto tardó en verse y qué habría
acortado la detección. Un fichero por incidencia, fechado; el formato está en
[`_plantilla.md`](_plantilla.md). No es un registro de bugs (eso es el §6 de
[`../sdd/README.md`](../sdd/README.md)): es el análisis de los fallos que
**llegaron a ejecución** y de los patrones que se repiten.

## Índice

| Fecha | Incidencia | Fingerprints | Estado |
|---|---|---|---|
| 2026-09-09 | [Primer arranque end-to-end: seis defectos de 53 días](2026-09-09-primer-arranque-e2e.md) | `sync-state:reentrada-no-permitida` ×2, `infra-local:contradice-a-la-app` ×3, `boot4:propiedad-movida-en-silencio`, `test:puerto-mockeado-oculta-invariante` | cerrada (causa raíz atacada en la Fase 1 de la auditoría) |
| 2026-09-12 | [La primera escritura en Elasticsearch rompía tras el salto a Boot 4.1](2026-09-12-es-semconv-classnotfound.md) | `test:sin-e2e-nadie-toca-la-infra`, `boot4:dependencia-transitiva-rota` | cerrada (`opentelemetry-semconv` 1.34.0 fijado) |
| 2026-09-23 | [Briefing inicial de OPS-010 entregaba `sqlserver-init.sql` con errores de T-SQL sobre TDS](2026-09-23-briefing-mssql-init-script.md) | `infra-local:contradice-a-la-app` (4.ª recurrencia) | abierta (incidente concreto cerrado por `6f6f175`; tarea de diseño abierta por la 4.ª recurrencia) |

## Índice de fingerprints

Un *fingerprint* es un identificador estable de **causa raíz**, no de síntoma:
si vuelve a aparecer, no es un bug nuevo, es una recurrencia, y la respuesta
correcta es atacar el diseño, no la fila. Regla: a la **segunda** recurrencia
se abre una tarea de diseño, no un arreglo puntual.

| Fingerprint | Causa raíz | Recurrencias | Última | Cerrado por |
|---|---|---|---|---|
| `sync-state:reentrada-no-permitida` | La máquina de estados se modeló para un pipeline y se extendió fila a fila; cada use case fijaba su estado de entrada y el repositorio recalculaba el `from` | **3** (`d1eba38→d9904cc`, `d9904cc→fa9b4e9`, auditoría B1/B2/B14) | 2026-09-10 | `beginCycle`/`advance` separados + test de propiedad estados × entradas ([`../sdd/common/maquina-de-estados.md`](../sdd/common/maquina-de-estados.md), Fase 1, `84a9da4`) |
| `test:puerto-mockeado-oculta-invariante` | Mockear un puerto que valida invariantes esconde justo los fallos que importan (D2, D6, V1, V2) | **5** (defectos D2/D6/V1/V2 + auditoría 2A-13: `article` sigue con `InMemoryStateRepo` solo en `customer`) | 2026-09-18 | **Abierto tras la 5.ª recurrencia** → tarea de diseño (C2-4, no arreglo puntual): mover `InMemoryStateRepo` a `common` y usarlo también en `article` |
| `test:sin-e2e-nadie-toca-la-infra` | Los slices y los smoke de contexto no escriben en Mongo/ES/Kafka; lo que falla al tocar la infraestructura solo se ve en vivo (D1, D3, D4, D5, semconv) | 6 + 1 | 2026-09-12 | **Abierto**: `SyncStateMongoIT` cubre Mongo; falta el e2e completo (TEST-1/TEST-4) |
| `infra-local:contradice-a-la-app` | El compose y los scripts de init describían otra versión, otro esquema u otro dialecto del que la app y los conectores reales consumen (ES 8 vs cliente 9, `init.js` con `id` vs `_id`, índice sin nombre; y ahora `sqlserver-init.sql` con `GO` y `USE` no válidos sobre TDS) | **4** | 2026-09-23 | **Abierta tras la 4.ª recurrencia** → tarea de diseño abierta: validar briefings que llevan SQL contra el contenedor MSSQL real antes de pasarlos al `dev-implementer`, o marcarlos como "SQL no validado" en la revisión. Las tres anteriores quedaron cerradas con arreglos puntuales (Compose 9.2.1, `init.js` con `_id` e índice parcial nombrado, imágenes por digest), pero la 4.ª demuestra que el arreglo puntual no protege del siguiente |
| `boot4:propiedad-movida-en-silencio` | Boot 4 movió `spring.data.mongodb.*` a `spring.mongodb.*` y la antigua se ignoraba sin aviso | 1 | 2026-09-10 | `CustomerMongoDatabaseConfigTest` (patrón «test de configuración», TEST-2) |
| `boot4:dependencia-transitiva-rota` | Una librería arrastra una versión de otra que no tiene la clase que usa (`elasticsearch-java` 9.4 + `semconv` 1.30.0-rc.1) | 1 | 2026-09-12 | Versión fijada en el parent; Dependabot avisará del siguiente salto |
| `test:seguridad-solo-probada-con-token` | `ApiSecurityTest` cubría la cadena activa; nadie probaba el modo abierto y en local todos los endpoints devolvían 403 | 1 | 2026-09-14 | `ApiSecurityDisabledTest`; anónimo con todos los roles en modo abierto |
| `openapi-generator:target-sucio` | Tres ejecuciones del generador borraban el `target` de la anterior | 3 (DX-7) | 2026-09-12 | `deleteOutputDirectory=false` en la 2.ª y 3.ª ejecución |
| `feature-pipeline:excepcion-en-send:sin-aviso-parcial` | El envío por parte del cliente compartía un único bucle sin `try` por parte: si una lanzaba (circuito abierto), las siguientes no se intentaban y el aviso de fallo parcial (ADR-0010) no llegaba a emitirse en su caso motivador | 1 (auditoría 2B-1) | 2026-09-18 | **Cerrado por: revisión 3, 2026-09-18** — cada parte en su propio `try`, cierra con su motivo, el agregado aplica cero confianza (D-14) y el aviso lleva la traza de pasos del ciclo |
| `sync-state:lectura-desfasada:dlt-sin-reintento` | Dos instancias con lecturas desfasadas de la cabecera de estado producían `IllegalStateException`, declarada no reintentable: el mensaje iba a la DLT sin un solo reintento | 1 (auditoría 2B-2) | 2026-09-18 | **Cerrado por: revisión 3, 2026-09-18** — el repositorio compara el `from` declarado con el real y el `cycleId` como *fencing token*; la colisión lanza `ConcurrentTransitionException` (reintentable), reservando `IllegalStateException` para transiciones ilegales de verdad (ADR-0011) |
| `sap-client:retry-post:duplicado` | El retry HTTP reintentaba cualquier método, incluido `POST`: un timeout de respuesta con SAP ya procesando podía crear el mismo Business Partner varias veces | 1 (auditoría 2B-3) | 2026-09-18 | **Cerrado por: revisión 3, 2026-09-18** — un `POST`/`PATCH`/`DELETE` solo se reintenta si el fallo ocurrió antes de que la petición saliera (clasificación por la cadena de causas real, no por tipo de excepción) |
| `odata:contact:payload-vacio` | El adaptador de contacto descartaba el dato recibido: el payload que mandaba a SAP no llevaba ni email ni teléfono | 1 (auditoría 2B-5, PRD-2) | 2026-09-18 | **Cerrado por: revisión 3, 2026-09-18** — email, teléfono, fax y web viajan ahora a las entidades de comunicación de la dirección |
| `odata:post-sin-upsert:duplicado` | Ningún adaptador OData preguntaba antes de escribir: cada re-sincronización tras un fallo, y cada reintento, creaba en vez de actualizar | 1 (auditoría 2B-6, PRD-11) | 2026-09-18 | **Cerrado por: revisión 3, 2026-09-18 (parcial)** — lookup previo y alta/actualización en los seis adaptadores; verificación contra el tenant aún pendiente |
| `btp:default-activo:servicio-inexistente` | La familia de adaptadores BTP es la activa por defecto y apunta a un contrato que el propio proyecto define, sin spec oficial ni servicio real integrado detrás | 1 (auditoría 2B-4) | 2026-09-18 | **Abierto**: BTP se mantiene por decisión **D-10** (el servicio del propietario existe y se probó aislado); mitigación propuesta **OPS-9** (guard de arranque que impida un `Btp*Adapter` activo sin `sap.btp.base-url` real) |
| `archunit:motor-junit5-sobre-platform-6:reglas-no-ejecutadas` | `archunit-junit5` está compilado contra JUnit Platform 1.x; con Spring Boot 4 (Platform 6) el motor no se registra y surefire 3.5.x tampoco lo lanza: `DomainPurityTest`, `ApplicationPurityTest` y `EndpointsDeclareAccessTest` salían con `Tests run: 0` y una regla imposible pasaba en verde. Las dos auditorías las dieron por vigilantes | 1 (hallazgo del cierre de la revisión 3; lo destapó el `Tests run: 0` del informe de línea base) | 2026-09-18 | **Cerrado por: revisión 3, 2026-09-18** — `archunit-junit6` 1.5.0 + surefire/failsafe 3.6.0; verificado con una sonda que debía fallar y falló; las 7 reglas reales pasan. Lección: un guard cuyo recuento es 0 no vigila nada; `TestCountMatchesDocsTest` no lo ve porque cuenta `@Test`, no `@ArchTest` |
| `sap-client:dns-como-illegal-argument:dlt-sin-reintento` | `UnresolvedAddressException` hereda de `IllegalArgumentException`, que el retry, el circuit breaker y Kafka trataban como error de programación: un fallo de DNS transitorio iba a la DLT sin reintentos | 1 (hallazgo de la revisión 3, no estaba en la auditoría del 18-09) | 2026-09-18 | **Cerrado por: revisión 3, 2026-09-18** — la clasificación de un fallo de transporte pasa a ser por predicado sobre la cadena de causas, no por el tipo de excepción de nivel superior |
