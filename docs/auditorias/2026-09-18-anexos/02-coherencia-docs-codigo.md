# Auditoría 2 — coherencia docs↔código y docs↔docs

Repo: `poc-sap-integration-java`, HEAD `ffd86e7` (2026-09-14). Solo lectura.
Fuente de verdad ante contradicción: el código.

## 1. Tabla de contradicciones

| # | Documento afirma X | Código / otro documento dice Y | Severidad |
|---|---|---|---|
| 1 | `AGENTS.md:237` "Sin autenticación todavía — brecha abierta" | `common/src/main/java/.../security/ApiSecurityConfig.java`, `ApiRoles.java`, `KeycloakRoleConverter.java` existen; los 8 endpoints reales llevan `@PreAuthorize` (`CustomerHistoryController.java:50,74`, `CustomerStateController.java:29`, `SyncCustomerController.java:36,52`, `ArticleHistoryController.java:35,56`, `SyncArticleController.java:26`); `docs/sdd/README.md:159` y `docs/auditorias/2026-09-12-cierre-bloqueantes.md:13` dicen B4 **cerrado** (Keycloak, ADR-0007) el 2026-09-12 | **bloqueante** — es el punto de entrada del repo; un agente que lo lea puede asumir que no hace falta declarar acceso en un endpoint nuevo, cuando `EndpointsDeclareAccessTest` rompe el build si falta. Mitigado parcialmente porque Parte 3 (`AGENTS.md:358-362`) sí exige `@PreAuthorize`, pero §2.5 sigue describiendo un estado que no existe |
| 2 | `AGENTS.md:228-236` tabla de endpoints REST no incluye `GET /customers/{id}/state` | `CustomerStateController.java:28` implementa `@GetMapping("/{id}/state")`; el endpoint está documentado en `docs/sdd/README.md:160`, `docs/sdd/customer/sincronizacion-cliente.md`, `docs/sdd/common/seguridad-api.md`, `docs/GLOSSARY.md` (entrada "Sincronización parcial"), `docs/operacion/RUNBOOKS.md` y `docs/architecture/adr/0010-...md` | **atencion** — el resto de la documentación sí lo recoge; solo el mapa de entrada omite el endpoint que la propia auditoría de cierre (B4/D-2) señala como pieza clave de la alerta de sincronización parcial |
| 3 | `AGENTS.md:220` "`IngestionPort` \| nadie lo implementa ... código muerto **pendiente de retirar** (auditoría A18)" | `grep -rn "IngestionPort" --include=*.java .` → **0 resultados** en todo el reactor. `docs/sdd/README.md:164` dice explícitamente "Borrados" para A18 (fecha 2026-09-12) | **bloqueante** — instrucción activamente falsa sobre una pieza de arquitectura: dice que hay que retirar algo que ya no existe, y contradice a `docs/sdd/README.md` §6, que es la fuente que el propio `AGENTS.md:39` señala como "índice vivo" |
| 4 | `docs/architecture/TECH.md:95` "`IngestionPort` \| **sin implementaciones**: ... código muerto, A18" y `TECH.md:106` "intercambiables a través del puerto `IngestionPort`" | Mismo hallazgo que el #3: 0 ocurrencias en el código. `IngestionMessage` (el DTO, no el puerto) sí existe y se sigue usando (`common/src/main/java/.../domain/IngestionMessage.java`) | **bloqueante** — TECH.md es el documento "cómo está construido"; describe un puerto que no está en el árbol de paquetes actual y confunde `IngestionPort` (borrado) con `IngestionMessage` (vivo) |
| 5 | `docs/GLOSSARY.md:66` "Callback ... Se modela como entrada alternativa en el puerto `IngestionPort`" | Mismo hallazgo #3/#4: el puerto no existe en el código | **atencion** — entrada de glosario que cita una pieza borrada; el término real vivo es `IngestionMessage` |
| 6 | `AGENTS.md:299-300` "**sin** starter OTel (incompatible — se usa el javaagent)" y `AGENTS.md:306` "trazas por **OTel javaagent**" | `common/pom.xml:46` declara `<artifactId>spring-boot-starter-opentelemetry</artifactId>`; `docs/architecture/adr/0009-trazas-con-el-starter-oficial-de-opentelemetry.md` (estado ✅ aceptada, 2026-09-12) decide explícitamente el **starter oficial**, apagado por defecto; `docs/architecture/TECH.md:198-200` (§9) también dice "starter oficial de Boot 4" | **bloqueante** — instrucción operativa que un agente seguiría al añadir observabilidad: le diría que use el javaagent cuando el proyecto ya adoptó, documentó (ADR-0009) y empaquetó (`pom.xml`) el starter oficial |
| 7 | `docs/GLOSSARY.md:272-274` "OpenTelemetry (OTel) ... Se integra con el **javaagent** ... (el starter Spring de OTel 2.x no soporta Boot 4)" | Mismo hallazgo que #6: contradice `common/pom.xml:46` y ADR-0009, que el propio glosario cita en la entrada vecina `OTLP` (`docs/GLOSSARY.md:278`, "Sin colector ni starter decidido (D-7)" — pero D-7 **sí** está decidida desde el 2026-09-12) | **bloqueante** — el glosario se contradice a sí mismo entre dos entradas consecutivas (OTel dice "no hay starter", OTLP dice "starter no decidido" cuando sí lo está) |
| 8 | `docs/auditorias/2026-09-12-cierre-bloqueantes.md:39` "7 Refactor \| ⬜ pendiente (**única fase que no ha empezado**)" | `docs/auditorias/2026-09-10-plan-de-accion.md:561` tabla de seguimiento: "7 · Refactor \| ✅ hecha (salvo resto de A5 ...) \| 2026-09-12 \| A4, A18, A19 (`d9ffce1`) · A5/A20 ... (`27fbbfc`)"; y el propio plan, sección "Fase 7" (`plan-de-accion.md:367-375`), describe el "Resultado (12-09-2026)" con commits reales | **bloqueante** — son los dos documentos de seguimiento de fases del mismo ciclo de auditoría, y dicen lo contrario sobre el mismo hito (empezada/terminada vs no empezada). Un agente que confíe en el documento de cierre creería que hay que rehacer una fase ya cerrada, o al revés, saltarse trabajo pendiente real (DX-8, que el propio plan reconoce como resto sin cerrar) |
| 9 | `AGENTS.md:220` y `docs/architecture/TECH.md:95` describen `IngestionPort` como código a retirar "**pendiente**" | `docs/auditorias/2026-09-12-cierre-bloqueantes.md` (Fase 7 en la tabla) no aclara si A18 se hizo en esa fase o en otra; pero `docs/sdd/README.md:164` y `plan-de-accion.md:363-365,373-374` sí registran el borrado como hecho el 2026-09-12 | **observacion** (redundante con #3/#4, listado para mostrar que tres documentos —AGENTS, TECH, GLOSSARY— arrastran el mismo dato caducado desde antes del cierre) |

## 2. Enlaces markdown rotos

Recorrido con un script Python de solo lectura (no escribe nada fuera del scratchpad) sobre todos los `.md` de la raíz y de `docs/`, resolviendo cada enlace relativo (excluidos `http(s)`, `#anchor`, `mailto:`) contra el sistema de ficheros.

**Resultado: 0 enlaces rotos.** La red de documentos está bien enlazada — no hay puntero muerto tras las 9 fases de arreglos.

## 3. Glosario — términos y entradas

- **Entrada obsoleta por contenido, no por ausencia**: `docs/GLOSSARY.md:66` (Callback) y `docs/GLOSSARY.md:272-278` (OpenTelemetry/OTLP) citan `IngestionPort` y "sin starter decidido" respectivamente — ya cubiertos en la tabla (#5, #6, #7). No son términos que falten; son definiciones que quedaron desactualizadas cuando cambió el código/ADR que describen.
- **Comparación con la auditoría anterior** (`docs/auditorias/2026-09-10-auditoria-agentes.md:315`, que decía "13 términos frecuentes sin entrada, 6 definiciones obsoletas, un comando de shell pegado dentro de una definición" y contradicción `.DLT` vs `-dlt`): el plan (`plan-de-accion.md:431`) registra "glosario con los 11 términos que faltaban" cerrado el 2026-09-12. Verificado: `DLT` (`docs/GLOSSARY.md:118-120`) ya usa `-dlt` consistentemente, sin discrepancia. No se encontró el "comando de shell pegado" citado por la auditoría anterior — parece corregido. No pude verificar exhaustivamente los 13 términos originales uno a uno (no tengo la lista literal de esa auditoría más allá de la cita agregada); dentro del tiempo disponible, los términos revisados manualmente (`IngestionPort`, `OTel`, `starter oficial`, `DLT`) están al día salvo los ya listados en la tabla.
- No se detectaron términos nuevos evidentes en el código (`ConcurrentTransitionException`, `ENTRY_STATES`, `SapCircuitOpenException`, `RetryBudgetGuard`, etc.) que falten en el glosario: todos tienen entrada.

## 4. Qué está bien

- La regla del ancla SDD se cumple en la práctica: los specs de `docs/sdd/**` citados en `AGENTS.md` y `docs/sdd/README.md` §5 existen todos en disco (`sincronizacion-cliente.md`, `baja-cliente.md`, `baja-mandato-sepa.md`, `sincronizacion-datos-bancarios.md`, `sincronizacion-direccion.md`, `maquina-de-estados.md`, `resiliencia-cliente-sap.md`, `seguridad-api.md`, `autenticacion-sap.md`, `observabilidad.md`, `idempotencia-y-dedupe.md`, `sincronizacion-articulo.md`) — cero specs fantasma.
- El recuento de tests (`docs/testing/TESTING.md:11`, "314 tests declarados") coincide exactamente con `grep -rn "^\s*@Test\b"` sobre el árbol actual (314): `TestCountMatchesDocsTest` está haciendo su trabajo.
- Cero enlaces markdown rotos en toda la documentación (§2).
- `scripts/start-all.sh`, `scripts/stop-all.sh`, `scripts/sdd-registry-check.py`, `mvnw`/`mvnw.cmd`/`.mvn/wrapper/`, `.github/workflows/ci.yml` — todos los comandos y ficheros que `AGENTS.md` §2.8 y §1.4 citan existen realmente en el árbol.
- La contradicción entre `cierre-bloqueantes.md` y `plan-de-accion.md` sobre la Fase 7 (#8) es la única discrepancia real encontrada entre esos dos documentos de seguimiento — el resto de fases (0-6, 8) coinciden en estado entre ambos ficheros.

## 5. Lo que NO verifiqué

- No ejecuté `mvn verify`, `TestCountMatchesDocsTest`, `EndpointsDeclareAccessTest` ni ningún test — el recuento y las reglas de `@PreAuthorize` se confirmaron por lectura estática (`grep`), no por build real.
- No comprobé exhaustivamente la lista literal de los "13 términos sin entrada / 6 definiciones obsoletas" de la auditoría del 2026-09-10 término a término contra el glosario actual; solo verifiqué los casos donde ya tenía evidencia de otra fuente (OTel, DLT, IngestionPort).
- No entré en `docs/sdd/customer/CHANGELOG.md`, `docs/sdd/article/CHANGELOG.md` ni `docs/sdd/common/CHANGELOG.md` línea a línea contra el `§10` de cada spec — solo confirmé que los ficheros existen.
- No audité `docs/operacion/RUNBOOKS.md`, `docs/operacion/APTITUD-PRODUCCION.md`, `docs/testing/CHECKLIST-TENANT-SAP.md`, `docs/tools-integrations/KEYCLOAK.md`, `docs/tools-integrations/SAP_CLOUD_SDK.md`, `docs/tools-integrations/MCP.md`, `deploy/README.md`, `docs/architecture/FLOWS.md`, `docs/architecture/INTEGRATION-PATTERNS.md`, `docs/architecture/MAPA-FUNCIONAL.html` ni los 10 ADR uno a uno contra el código — el alcance cubierto (AGENTS.md completo, endpoints, puertos, stack, sdd/README, GLOSSARY, TODO/MEJORAS, las dos auditorías, enlaces) agota el presupuesto de esta sesión.
- No comparé `TODO.md` vs `docs/MEJORAS-Y-PROPUESTAS.md` item a item más allá de una lectura completa de ambos encabezados y tablas principales; no encontré duplicados con estado contradictorio evidente entre los dos (el solapamiento visible, p. ej. Keycloak/SEC-1, es coherente: MEJORAS registra "código implementado" y TODO registra "configuración externa pendiente en el Keycloak corporativo", que son cosas distintas), pero no descarto que exista alguno más fino que requiera comparar cada fila.
- No verifiqué en vivo (build, arranque de la app, tenant SAP) nada de lo anterior: es una auditoría de solo lectura sobre texto y código estático.
